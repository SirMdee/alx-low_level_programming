#include <stdio.h>
/**
 * main - main
 * Return: 0 (Success) Always
 */
#include <stdio.h>
int main(void)
{
char *text = "Programming is like building a multilingual puzzle";
puts(text);
return (0);
}
